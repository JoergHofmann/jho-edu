class inventar {
   constructor(d, g) { //Datum, Güter
       this.datum = d;
       this.gueter = g;
       this.anzahl = g.length;
       this.folge = f;
   }

   mischen = function () {
   }

   anzeigen = function () {
   }
}
